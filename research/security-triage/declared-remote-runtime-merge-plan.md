# Declared remote runtime merge plan

Merge this branch together with the matching `mcp-native-guard` and `mcp-observatory-guard-portal` branches. The first production deployment should retain the existing runtime timer/service, use a small remote batch, verify state distribution and ensure local runtime collection remains unaffected.
