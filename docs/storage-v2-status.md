# Storage v2 MVP branch status

This branch is intended for side-by-side validation only. It does not install or enable production timers, replace the published catalog, delete legacy evidence, or modify the current public portal/Nginx configuration.

The production-side acceptance sequence is in `docs/storage-v2-mvp-test-plan.md`.
