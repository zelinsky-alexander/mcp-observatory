# Storage v2 dependency note

Storage v2 introduces no new third-party runtime dependency. The implementation uses the repository's existing SQLite dependency plus Python/C++ standard-library APIs and existing project tooling. No new vendored source or copied third-party implementation is included by this milestone.
